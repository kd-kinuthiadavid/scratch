/**
 * @TODO: @FIXME:
 * ADD the base imagekit url to doppler and use it as an environment variable
 */
const imagesLoaer = (src: string): string =>
  `https://ik.imagekit.io/3t80u6sjo/SCRATCH/Scratch/${src}`;

export { imagesLoaer };
